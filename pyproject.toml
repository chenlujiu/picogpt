[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "picogpt"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "maturin>=1.12.4",
    "notebook>=7.5.3",
    "numpy>=2.4.2",
    "torch>=2.10.0",
]
