[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "picogpt"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "maturin>=1.12.4",
    "numpy>=2.4.2",
    "pyarrow>=23.0.1",
    "requests>=2.32.5",
    "tiktoken>=0.12.0",
    "torch>=2.10.0",
    "wandb>=0.25.0",
]
